{% extends "base.html" %}

{% block content %}
    <h1>Hello, {{ user.nickname }}!</h1>
    {% for post in posts %}
    <div>Post by {{ post.author.nickname}}<p><b>{{ post.body }}</b></p></div>
    {% endfor %}
{% endblock %}
